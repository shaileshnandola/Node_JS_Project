<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Book Catalog</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f0f2f5;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        .book-card {
            max-width: 700px;
            margin: 50px auto;
            border-radius: 12px;
            overflow: hidden;
            background-color: #fff;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
            transition: transform 0.2s, box-shadow 0.2s;
        }

        .book-image {
            width: 100%;
            height: 300px;
            object-fit: cover;
        }

        .book-details {
            padding: 25px 20px;
        }

        .book-details h4 {
            color: #0d6efd;
            font-size: 1.7rem;
            font-weight: 600;
            margin-bottom: 15px;
        }

        .book-details p {
            font-size: 1rem;
            margin-bottom: 8px;
            color: #495057;
        }

        .book-details p span {
            font-weight: 500;
            color: #212529;
        }

        .btn-back {
            display: inline-block;
            margin-top: 15px;
            border-radius: 6px;
            background-color: #0d6efd;
            color: #fff;
            padding: 8px 18px;
            text-decoration: none;
        }

        .btn-back:hover {
            background-color: #0b5ed7;
            color: #fff;
        }
    </style>
</head>

<body>
    <div class="book-card">
        <img src="<%= task.image %>" class="book-image" alt="Book Image">
        <div class="book-details">
            <h4>
                <%= task.title %>
            </h4>
            <p><span>Author:</span>
                <%= task.author %>
            </p>
            <p><span>Genre:</span>
                <%= task.genre %>
            </p>
            <p><span>Total Copies:</span>
                <%= task.total %>
            </p>
            <p><span>Description:</span>
                <%= task.description %>
            </p>
            <a href="/viewtodo" class="btn-back">Back</a>
        </div>
    </div>
</body>

</html>