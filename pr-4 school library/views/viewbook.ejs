<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Book Catalog</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .book-img {
            width: 40px;
            height: 60px;
            object-fit: cover;
            margin-right: 10px;
            border-radius: 4px;
            transition: transform 0.2s ease-in-out;
        }

        .book-img:hover {
            transform: scale(1.1);
        }

        .table-hover tbody tr:hover {
            background-color: #f8f9fa;
        }

        .btn-sm {
            margin-right: 5px;
        }
    </style>
</head>

<body class="bg-light">
    <div class="container">
        <div class="row">
            <div class="col-md-2 bg-dark text-white min-vh-100 p-3">
                <h4 class="text-center mb-4">LibraryMS</h4>
                <ul class="nav flex-column">
                    <li class="nav-item"><a href="/" class="nav-link active bg-primary text-white">Book Catalog</a></li>
                    <li class="nav-item"><a href="/todo" class="nav-link text-white">Add New Book</a></li>
                </ul>
            </div>
            <div class="col-md-10 p-4">
                <div class="d-flex justify-content-between align-items-center mb-4">
                    <h3>📚 Book Catalog</h3>
                    <a href="/todo" class="btn btn-success">+ Add Book</a>
                </div>

                <table class="table table-bordered table-hover bg-white shadow-sm">
                    <thead class="table-light">
                        <tr>
                            <th>Title</th>
                            <th>Author</th>
                            <th>ISBN</th>
                            <th>Genre</th>
                            <th>Total</th>
                            <th>Description</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% alltaskdata.map((book)=> { %>
                            <tr>
                                <td>
                                    <div class="d-flex align-items-center">
                                        <img src="<%= book.image %>" class="book-img">
                                        <span>
                                            <%= book.title %>
                                        </span>
                                   </div>
                                </td>
                                <td>
                                    <%= book.author %>
                                </td>
                                <td>
                                    <%= book.isbn %>
                                </td>
                                <td>
                                    <%= book.genre %>
                                </td>
                                <td>
                                    <%= book.total %>
                                </td>
                                <td>
                                    <%= book.description %>
                                </td>
                                <td>
                                    <a href="/task/<%= book._id %>" class="btn btn-info btn-sm me-1">View</a>
                                    <a href="/edit/<%= book._id %>" class="btn btn-warning btn-sm">Edit</a>
                                    <a href="/delete/<%= book._id %>" class="btn btn-danger btn-sm">Delete</a>
                                </td>
                            </tr>
                            <% }) %>
                    </tbody>

                </table>
            </div>

        </div>
    </div>
</body>

</html>