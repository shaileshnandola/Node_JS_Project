<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Add New Book</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    body {
      background-color: #f8f9fa;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      margin: 0;
    }

    .container {
      max-width: 1400px;
      margin: 0 auto;
    }

    .layout {
      display: flex;
      min-height: 100vh;
    }

    .sidebar {
      width: 220px;
      background: #212529;
      padding: 20px;
      color: #fff;
    }

    .sidebar h4 {
      font-weight: bold;
      margin-bottom: 30px;
      font-size: 1.6rem;
    }

    .sidebar .nav-link {
      color: #fff;
      margin-bottom: 10px;
      padding: 8px 12px;
      border-radius: 5px;
      display: block;
      transition: all 0.2s;
    }

    .sidebar .nav-link:hover,
    .sidebar .nav-link.active {
      background-color: #0d6efd;
      color: #fff;
    }

    .content {
      padding: 30px;
      width: 100%;
    }

    .card {
      border-radius: 8px;
      box-shadow: 0 2px 6px rgba(0, 0, 0, 0.1);
    }

    .form-control {
      border-radius: 6px;
    }

    .btn-success,
    .btn-secondary {
      border-radius: 6px;
    }

    .d-flex.gap-2 {
      margin-top: 10px;
    }
  </style>
</head>

<body>
  <div class="container">
    <div class="layout">

      <div class="sidebar">
        <h4>LibraryMS</h4>
        <nav class="nav flex-column">
          <a class="nav-link" href="/viewtodo">Book Catalog</a>
          <a class="nav-link active" href="/todo">Add New Book</a>
        </nav>
      </div>

      <div class="content">
        <h3 class="mb-4">Add New Book</h3>
        <div class="card p-4">
          <form action="/addtodo" method="POST" enctype="multipart/form-data">

            <div class="mb-3">
              <input type="text" class="form-control" id="title" name="title" placeholder="Enter Book Title" required>
            </div>
            <div class="mb-3">
              <input type="text" class="form-control" id="author" name="author" placeholder="Enter Author Name"
                required>
            </div>

            <div class="mb-3">
              <textarea name="description" id="description" class="form-control shadow-none" rows="1"
                placeholder="Enter Description" ></textarea>
            </div>

            <div class="mb-3">
              <input type="text" class="form-control" id="genre" name="genre" placeholder="Enter Genre" required>
            </div>
            <div class="mb-3">
              <input type="number" class="form-control" id="total" name="total" placeholder="Enter Total Copies" min="1"
                required>
            </div>

            <div class="mb-3">
              <input type="file" class="form-control" id="image" name="image">
            </div>

            <div class="d-flex gap-2">
              <button type="submit" class="btn btn-success"><i class="fa-solid fa-plus me-1"></i> Add Book</button>
              <a href="/viewtodo" class="btn btn-secondary"><i class="fa-solid fa-arrow-left me-1"></i> Back</a>
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>
</body>

</html>